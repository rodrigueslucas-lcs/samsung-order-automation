# Market-owned automation

Current executable regional automation lives here.

```text
markets/
  mx/{qst,dst}/
  pe/{qst,dst}/
```

Environment is runtime configuration, not a folder. Do not create `s1`/`s2` subtrees.

CL/CO can be added here when their runtime implementations are enabled.
